{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate --schema packages/db/prisma/schema.prisma && node scripts/migrate.js && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"],
  "headers": [
    {
      "source": "/api/webhooks/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
