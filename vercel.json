{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "cd packages/db && npx prisma generate && cd ../.. && cd apps/web && npx next build",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/heygen-recover",
      "schedule": "0 2 * * *"
    }
  ],
  "headers": [
    {
      "source": "/api/webhooks/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
