{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate --schema packages/db/prisma/schema.prisma && export DIRECT_URL=$(echo $DATABASE_URL | sed 's/-pooler\\./-./g') && DATABASE_URL=$DIRECT_URL npx prisma migrate deploy --schema packages/db/prisma/schema.prisma && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"],
  "headers": [
    {
      "source": "/api/webhooks/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
